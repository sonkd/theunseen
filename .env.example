# No environment variables are currently required to run this project
# (dev, build, or the content pipeline scripts). This file exists as a
# placeholder so future env vars have a documented home — copy to `.env`
# and fill in if/when one is actually added.
